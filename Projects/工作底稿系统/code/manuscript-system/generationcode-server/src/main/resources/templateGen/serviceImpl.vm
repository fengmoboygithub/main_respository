package ${serviceImplUrl};

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.company.base.dao.IBaseDao;
import com.company.base.service.BaseServiceImpl;
import ${serviceUrl}.${name}Service;
import ${mapperUrl}.${name}Mapper;
import ${url};
import ${exampleurl};

@Service
public class ${name}ServiceImpl extends BaseServiceImpl<${name}, ${name}Example> implements ${name}Service<${name}, ${name}Example>{

    @Autowired
    private ${name}Mapper mapper;
	
	public IBaseDao<${name}, ${name}Example> getMapper(){
		return mapper;
	}
}